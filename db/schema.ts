import { integer,sqliteTable,text,uniqueIndex } from "drizzle-orm/sqlite-core";
export const journalPosts=sqliteTable("journal_posts",{id:text("id").primaryKey(),regionSlug:text("region_slug").notNull(),slug:text("slug").notNull(),title:text("title").notNull(),excerpt:text("excerpt").notNull().default(""),body:text("body").notNull().default(""),tag:text("tag").notNull().default("COMMUNITY"),author:text("author").notNull().default("Regional Hub"),status:text("status").notNull().default("draft"),publishedAt:text("published_at"),createdAt:text("created_at").notNull(),updatedAt:text("updated_at").notNull()},table=>({regionSlugUnique:uniqueIndex("journal_posts_region_slug_unique").on(table.regionSlug,table.slug)}));
export const socialConnections=sqliteTable("social_connections",{id:text("id").primaryKey(),regionSlug:text("region_slug").notNull(),provider:text("provider").notNull(),profileUrl:text("profile_url").notNull().default(""),handle:text("handle").notNull().default(""),enabled:integer("enabled",{mode:"boolean"}).notNull().default(false),updatedAt:text("updated_at").notNull()},table=>({regionProviderUnique:uniqueIndex("social_connections_region_provider_unique").on(table.regionSlug,table.provider)}));
