# Public identity for this deployment. Every regional domain is a separate
# deployment target from the same repository.
NEXT_PUBLIC_REGION_SLUG=africa
NEXT_PUBLIC_SITE_URL=https://kudihub.com

# Persistent SQLite database file. Relative paths resolve from the project root.
SQLITE_PATH=./data/app.db

# Application-owned manager logins. Generate real values with `npm run auth:setup`.
# Keep this JSON credential list and the session secret server-side.
ADMIN_CREDENTIALS_JSON=[]
AUTH_SESSION_SECRET=

# Staking ships disabled. Before enabling, verify the proxy implementation and
# ABI against docs/pool/STAKE_SPEC.md and configure every approved value below.
NEXT_PUBLIC_STAKING_ENABLED=false
NEXT_PUBLIC_CONFLUX_NETWORK=espace-mainnet
NEXT_PUBLIC_CONFLUX_CHAIN_ID=1030
NEXT_PUBLIC_CONFLUX_RPC_URL=https://evm.confluxrpc.com
NEXT_PUBLIC_STAKING_CONTRACT=0x3cbc6F7D406fe9701573FE6DdF28f4F17b5d46A3

# Official social-feed credentials. Profile links and safe fallback cards work
# without these; automatic Instagram/X feeds require the provider credentials.
INSTAGRAM_ACCESS_TOKEN=
X_BEARER_TOKEN=

# YouTube RSS works without a token when the manager supplies a /channel/UC… URL.
YOUTUBE_API_KEY=

# Production blockchain integration placeholders.
CFX_NETWORK_ID=
CFX_RPC_URL=
CFX_STAKING_CONTRACT_ADDRESS=
CFX_WALLETCONNECT_PROJECT_ID=
