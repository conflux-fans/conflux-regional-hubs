const escapeHtml=(value:string)=>value.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#039;");
export function safeExternalUrl(value:string):string|null{try{const url=new URL(value);return url.protocol==="http:"||url.protocol==="https:"?url.toString():null}catch{return null}}
function inline(value:string):string{let output=escapeHtml(value);output=output.replace(/`([^`]+)`/g,"<code>$1</code>").replace(/\*\*([^*]+)\*\*/g,"<strong>$1</strong>").replace(/_([^_]+)_/g,"<em>$1</em>").replace(/\[([^\]]+)\]\(([^)]+)\)/g,(_match,label:string,href:string)=>{const safe=safeExternalUrl(href);return safe?`<a href="${escapeHtml(safe)}" target="_blank" rel="noopener noreferrer">${label}</a>`:label});return output}
export function markdownToHtml(markdown:string):string{const lines=markdown.replace(/\r\n?/g,"\n").split("\n"),output:string[]=[];let listOpen=false,paragraph:string[]=[];const closeList=()=>{if(listOpen){output.push("</ul>");listOpen=false}},closeParagraph=()=>{if(paragraph.length){output.push(`<p>${inline(paragraph.join(" "))}</p>`);paragraph=[]}};for(const line of lines){if(!line.trim()){closeParagraph();closeList();continue}const heading=line.match(/^(#{1,3})\s+(.+)$/);if(heading){closeParagraph();closeList();const level=heading[1].length;output.push(`<h${level}>${inline(heading[2])}</h${level}>`);continue}const item=line.match(/^[-*]\s+(.+)$/);if(item){closeParagraph();if(!listOpen){output.push("<ul>");listOpen=true}output.push(`<li>${inline(item[1])}</li>`);continue}if(line.startsWith("> ")){closeParagraph();closeList();output.push(`<blockquote>${inline(line.slice(2))}</blockquote>`);continue}paragraph.push(line.trim())}closeParagraph();closeList();return output.join("\n")}
export function slugify(value:string):string{return value.toLowerCase().normalize("NFKD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,80)}
export function suggestedSlug(title:string,currentSlug:string,manuallyEdited:boolean):string{return manuallyEdited?currentSlug:slugify(title)}
