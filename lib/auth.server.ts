import { getChatGPTUser } from "../app/chatgpt-auth";export async function canManage():Promise<boolean>{if(process.env.NODE_ENV!=="production")return true;const user=await getChatGPTUser();if(!user)return false;const{env}=await import("cloudflare:workers");const runtime=env as unknown as{MANAGER_ALLOWED_EMAILS?:string};const allowlist=(runtime.MANAGER_ALLOWED_EMAILS||"").split(",").map(value=>value.trim().toLowerCase()).filter(Boolean);return !allowlist.length||allowlist.includes(user.email.toLowerCase())}
