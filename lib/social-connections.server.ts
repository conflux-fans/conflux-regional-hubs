import { region,type SocialProvider } from "../config/regions";export type SocialConnection={provider:SocialProvider;label:string;profileUrl:string;handle:string;enabled:boolean};async function database(){const{env}=await import("cloudflare:workers");return(env as unknown as{DB?:D1Database}).DB}export async function listSocialConnections():Promise<SocialConnection[]>{const defaults=(Object.entries(region.socials) as Array<[SocialProvider,(typeof region.socials)[SocialProvider]]>).map(([provider,value])=>({provider,...value,enabled:Boolean(value.profileUrl)}));const db=await database();if(!db)return defaults;const result=await db.prepare("SELECT provider,profile_url,handle,enabled FROM social_connections WHERE region_slug = ?").bind(region.slug).all<Record<string,unknown>>();const saved=new Map((result.results||[]).map(row=>[String(row.provider),row]));return defaults.map(connection=>{const row=saved.get(connection.provider);return row?{...connection,profileUrl:String(row.profile_url||""),handle:String(row.handle||""),enabled:Boolean(row.enabled)}:connection})}export async function saveSocialConnections(connections:SocialConnection[]):Promise<SocialConnection[]>{const db=await database();if(!db)throw new Error("Database binding DB is not configured.");const now=new Date().toISOString();await db.batch(connections.map(connection=>db.prepare("INSERT INTO social_connections (id,region_slug,provider,profile_url,handle,enabled,updated_at) VALUES (?,?,?,?,?,?,?) ON CONFLICT(region_slug,provider) DO UPDATE SET profile_url=excluded.profile_url,handle=excluded.handle,enabled=excluded.enabled,updated_at=excluded.updated_at").bind(`${region.slug}-${connection.provider}`,region.slug,connection.provider,connection.profileUrl,connection.handle,connection.enabled?1:0,now)));return listSocialConnections()}
