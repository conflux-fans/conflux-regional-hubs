import { region } from "../config/regions";
export type JournalPost={id:string;regionSlug:string;slug:string;title:string;excerpt:string;body:string;tag:string;author:string;status:"draft"|"published";publishedAt:string|null;createdAt:string;updatedAt:string};type D1Row=Record<string,unknown>;async function database(){const{env}=await import("cloudflare:workers");return(env as unknown as{DB?:D1Database}).DB}function fromRow(row:D1Row):JournalPost{return{id:String(row.id),regionSlug:String(row.region_slug),slug:String(row.slug),title:String(row.title),excerpt:String(row.excerpt||""),body:String(row.body||""),tag:String(row.tag||"COMMUNITY"),author:String(row.author||"Regional Hub"),status:row.status==="published"?"published":"draft",publishedAt:row.published_at?String(row.published_at):null,createdAt:String(row.created_at||""),updatedAt:String(row.updated_at||"")}}export function seededPosts():JournalPost[]{return region.journal.stories.map((story,index)=>({id:`seed-${index+1}`,regionSlug:region.slug,slug:story.slug,title:story.title,excerpt:story.deck,body:story.body,tag:story.tag,author:story.author,status:"published",publishedAt:new Date(story.date).toISOString(),createdAt:new Date(story.date).toISOString(),updatedAt:new Date(story.date).toISOString()}))}export async function listPosts(includeDrafts=false):Promise<JournalPost[]>{const db=await database();if(!db)return seededPosts();const result=await db.prepare(`SELECT * FROM journal_posts WHERE region_slug = ? ${includeDrafts?"":"AND status = 'published'"} ORDER BY COALESCE(published_at, updated_at) DESC`).bind(region.slug).all<D1Row>();const stored=(result.results||[]).map(fromRow);return stored.length?stored:seededPosts()}export async function getPost(slug:string):Promise<JournalPost|null>{const db=await database();if(db){const row=await db.prepare("SELECT * FROM journal_posts WHERE region_slug = ? AND slug = ? LIMIT 1").bind(region.slug,slug).first<D1Row>();if(row)return fromRow(row)}return seededPosts().find(post=>post.slug===slug)||null}export async function savePost(input:Partial<JournalPost>&Pick<JournalPost,"title"|"slug"|"body"|"status">):Promise<JournalPost>{const db=await database();if(!db)throw new Error("Database binding DB is not configured.");const now=new Date().toISOString(),id=input.id||crypto.randomUUID(),publishedAt=input.status==="published"?(input.publishedAt||now):null;await db.prepare(`INSERT INTO journal_posts (id,region_slug,slug,title,excerpt,body,tag,author,status,published_at,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(region_slug,slug) DO UPDATE SET title=excluded.title,excerpt=excluded.excerpt,body=excluded.body,tag=excluded.tag,author=excluded.author,status=excluded.status,published_at=excluded.published_at,updated_at=excluded.updated_at`).bind(id,region.slug,input.slug,input.title,input.excerpt||"",input.body,input.tag||"COMMUNITY",input.author||region.siteName,input.status,publishedAt,input.createdAt||now,now).run();return(await getPost(input.slug))!}
