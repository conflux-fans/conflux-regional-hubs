import { Footer, Header } from "../site-components";
import { region } from "../../config/regions";
export default function Stake(){return <main className={`stake-page stake-page-${region.presentation.pages.stake}`}><Header/><section className="inner-hero"><p className="eyebrow">{region.stake.eyebrow}</p><h1>{region.stake.headline}</h1><p>{region.stake.introduction} This review build shows the production interaction points without connecting a live wallet or contract.</p></section><section className="staking-panel"><div className="stake-card"><div><p className="eyebrow">SHARED STAKING EXPERIENCE</p><h2>Secure by design.<br/>Local by context.</h2><p>The production regional site will use the approved shared Conflux staking integration. Contract addresses, network checks, transaction status, and wallet connection stay in developer-controlled code.</p><p className="save-note">Review mode: no funds move and no wallet request is made.</p></div><div className="connect-box"><div><p className="eyebrow">WALLET STATUS</p><strong>Not connected</strong></div><button className="button light" type="button">Connect wallet <span>↗</span></button><small>Production connection required: approved Conflux network, staking contract and wallet provider.</small></div></div></section><Footer/></main>}
